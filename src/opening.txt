def getOpeningMoves(moves):
    valid_openings = []
    with open("\\Users\\willi\\OneDrive\\Documents\\Chess games\\Homemade_chess_bot\\openings.tsv", "r") as openings:
        reader = csv.reader(openings, delimiter = "\t")
        for row in reader:
            valid = True
            openingMoves = row[3].split(" ")
            if len(moves) >= len(openingMoves):
                continue
            for i in range(len(moves)):
                if moves[i] != openingMoves[i]:
                    valid = False
            if valid:
                valid_openings.append(openingMoves[len(moves)]) #if all the moves played match the moves in the opening, add the next move from the opening to the valid_moves

    return valid_openings   